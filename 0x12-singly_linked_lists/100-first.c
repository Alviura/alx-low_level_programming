#include <stdio.h>
/**
 * _printf - prints the statement below
 * Return: Always 0
 */
/**
 * main - check the code
 * Return: Always 0
 */
int _printf(void)
{
	printf("You're beat! and yet, you must allow,\n
		I bore my house upon my back!\n");
	return (0);
}

int main(void)
{
	_printf();
	printf("(A tortoise, having pretty good sense of a hare's nature,
       		challenges one to a race.)\n");
	return (0);
}
